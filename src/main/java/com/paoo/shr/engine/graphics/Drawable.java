package com.paoo.shr.engine.graphics;

import javafx.scene.canvas.GraphicsContext;

/**
 * Interfata care reprezinta un obiect care se poate randa.
 */
public interface Drawable {
    void draw(GraphicsContext gc);
}
